
export const SKILL_DRAFT_WRITING = `---
name: 正文写作流程
category: 创作
tools: ['outline', 'character', 'memory']
summarys: ['完整的小说正文创作工作流。从大纲检查、角色状态同步、记忆规则查询，到章节撰写、润色、字数检查的端到端流程。']
tags: ["技能", "正文", "写作", "创作", "写章节", "润色", "workflow"]
---

# 核心指令：正文写作专家

## 触发条件（主动判断）
当你发现以下情况时，**主动激活此技能**：
*   用户要求写某一章或某一段正文
*   用户说"开始写"、"写正文"、"写第X章"
*   用户要求续写、补写、或重写正文内容

---

## 工作流总览

P1 创作准备 → P2 撰写正文 → P3 润色优化 → P4 收尾检查

**此技能被激活时，已自动解锁 outline、character、memory 工具类别，无需再调用 search_tools。**

---

## P1：创作准备（必须先做，不可跳过）

### 1.1 同步大纲状态

**必须调用** outline 工具确认当前章节位置：

1. outline_getVolumes() — 查看卷结构
2. outline_getChapters() — 查看章节列表，定位当前要写的是第几章
3. outline_getEvents(chapterIndex) — 查看该章节已有的事件规划

**关键判断**（写正文前必须确认）：
- **如果该章节还没有事件规划** → 先调用 processOutlineInput 创建事件，**不要直接写正文**
- **如果大纲中该章节的事件为空或过于简略** → 先补充大纲事件内容，明确本章要发生什么
- **如果已有事件** → 记录事件标题、涉及角色、情绪基调、计划字数

> 禁止行为：大纲里没有本章剧情就动笔写。没有事件规划 = 没有创作方向。

### 1.2 读取上一章正文（剧情衔接检查）

**必须调用** readFile 读取上一章正文文件：

1. 通过 glob(05_正文草稿/*) 或 listFiles 找到上一章的文件路径
2. readFile(上一章路径) — 确认上一章结尾的剧情状态

**衔接检查清单**：
- 上一章结尾的钩子是什么？本章开头必须承接这个钩子
- 上一章结束时角色的状态？（位置、伤势、情绪、关系变化）
- 上一章埋下的新伏笔？本章是否需要呼应或推进？
- 上一章承诺给读者但还没交付的内容？（如"下章揭晓"类预告）

> 禁止行为：不读上一章就直接写，导致剧情断层、角色状态矛盾。

### 1.3 查询未收尾伏笔

**必须调用** outline_getUnresolvedForeshadowing() — 获取所有未完结的伏笔列表：

**判断**：
- 有已到期该回收的伏笔？→ 本章安排回收
- 有近期要推进的伏笔？→ 本章给线索或暗示
- 没有可用伏笔？→ 考虑在本章埋一个新伏笔
- 发现某个伏笔的计划回收章不合理？→ outline_manageForeshadowing(action: "update") 调整 plannedChapter

**操作**：
- 如需回收/推进某个伏笔 → outline_getForeshadowingDetail(foreshadowingId) 查看详情
- 如需修改伏笔属性（内容/计划回收章/强度等）→ outline_manageForeshadowing(action: "update", foreshadowingId, updates)
- 如需删除废弃伏笔 → outline_manageForeshadowing(action: "delete", foreshadowingId)
- 如需在本章埋新伏笔 → 在写正文时自然植入，写完后通过 outline_manageEvents 更新事件的 foreshadowing 字段

### 1.4 同步角色状态

**必须调用** character 工具查询本章涉及的所有角色：

1. query_relationships(characterName) — 查看角色当前关系网络
2. 读取 02_角色档案/前缀_角色名.md — 查看角色静态设定
3. 如果角色有动态档案（readFile 时会自动注入动态状态）→ 记录当前状态

**关键检查**：
- 角色在本章开始时的状态是什么？（伤势、情绪、关系变化）
- 角色在本章结束时的目标状态是什么？
- 状态变化是否有合理的因果链？

### 1.5 查询写作规则

**按需调用** memory 工具查询长期规则：

1. query_memory(keywords: ["写作规则", "语气", "禁忌"]) — 查询是否有用户设定的特殊写作规则
2. query_memory(keywords: ["世界观", "力量体系"]) — 查询世界观相关约束

**不要查询故事内容或角色信息** — 这些通过 readFile 获取，不存记忆宫殿。

---

## P2：撰写正文

### 2.1 创作约束确认

动笔前用双轴快速自检（无需调用 deep_thinking，简单任务直接判断）：

| 检查项 | 标准 |
|-------|------|
| 驱动力 | 本章靠什么让读者翻页？悬念？情感？发现？ |
| 目的性 | 删掉本章，读者少获得什么？ |
| 情绪曲线 | 从什么情绪值开始，到什么情绪值结束？ |
| 钩子 | 章末是否有明确的阅读期待？ |

### 2.2 写作规范

**文件命名**：05_正文草稿/卷[X]_章[X]_[章节名].md

**Frontmatter 模板**：

    ---
    summarys: []
    tags: ["正文"]
    characters: ["角色A", "角色B"]
    ---

**字数要求**：
- 每章 3000-5000 字
- 写完后用 searchFiles 或 readFile 检查字数

**节奏控制**：
- 前 20% 必须有即时冲突或强吸引力
- 中间 50% 推进核心事件，至少一个转折点
- 后 30% 高潮 + 章末钩子

### 2.3 工具调用顺序

撰写正文时只使用基础文件工具（write/edit/read），不需要 outline/character/memory 工具。

---

## P3：润色优化（撰写后必须执行）

### 3.1 连贯性检查

1. **人物一致性**：本章角色行为是否符合其档案设定？
2. **情节连贯**：与上一章的衔接是否自然？状态变化是否有交代？
3. **节奏控制**：是否有连续多段无冲突的"平铺直叙"？

### 3.2 去除 AI 痕迹

**必须检查并修改**：
- ❌ 过度修饰形容词（"璀璨"、"瑰丽"、"绚烂"）→ 删减或用具体细节替代
- ❌ 抽象陈述（"心中涌起复杂的情感"）→ 改为具体动作/对话
- ❌ 四字格律堆砌（"心潮澎湃、热血沸腾"）→ 打破节奏，用口语化表达
- ❌ 书面语套话 → 增加个性化对话
- ❌ 长句单调 → 长短句交替
- ❌ 笼统描述 → 具体的视觉/听觉/嗅觉细节

### 3.3 钩子检查

章末必须留下至少一种钩子：
- 悬念钩子：未解之谜
- 危机钩子：角色陷入险境
- 转折钩子：预期被打破
- 期待钩子：新的目标/诱惑

---

## P4：收尾检查

### 4.1 更新大纲与伏笔

**必须调用** outline 工具同步进度：

1. 如果本章事件已经存在 → outline_manageEvents(action: "update") 更新事件状态为"已完成"
2. 如果本章是新事件 → processOutlineInput 将本章内容转化为结构化事件
3. 如果本章埋了新伏笔或推进/回收了伏笔 → 确保通过 outline_manageEvents 的 foreshadowing 字段同步到 Timeline
4. 如果发现伏笔计划回收章需要调整（如剧情变化导致提前或延后）→ outline_manageForeshadowing(action: "update_planned", batchUpdates) 批量调整

### 4.2 更新角色状态

**必须调用** character 工具更新角色动态：

1. update_character_profile(characterName) — 分析本章内容，自动更新角色状态
2. 检查是否有新关系产生或旧关系变化 → manage_relationships

### 4.3 标记 TODO 完成

如果当前章节有对应的 TODO：manageTodos(action: "complete", indices: [X])

---

## 反模式（正文写作常见错误）

| 反模式 | 表现 | 纠正方法 |
|--------|------|---------|
| **跳过准备直接写** | 不看大纲、不查角色状态就动笔 | 严格执行 P1 流程 |
| **脱离大纲放飞** | 写的剧情和大纲事件对不上 | 写前确认事件，写后更新事件 |
| **角色状态断层** | 上一章重伤，这章满血无解释 | 查角色档案，状态变化要有交代 |
| **AI 味浓** | 形容词堆砌、抽象陈述、套话 | P3 润色清单逐条检查 |
| **无钩子收尾** | 章节结束得像段落结束 | 强制要求章末钩子 |
| **字数不足** | 低于 3000 字 | 扩充细节、增加场景、深化冲突 |
| **工具人配角** | 配角只服务于主角，没有主体性 | 给配角至少一个独立动机 |

---

## 与其他 Skill 的关系

- **深度思考方法论**：复杂章节（多线交汇、重大转折）写前可调用 deep_thinking 做约束分析
- **场景描写**：需要强化环境氛围时 activate_skill("场景描写")
- **对话写作**：对话占比过高或缺乏个性时 activate_skill("对话写作")
- **战斗场景**：涉及战斗时 activate_skill("战斗场景")
- **情感渲染**：情感高潮章节 activate_skill("情感渲染")
- **文本润色**：完成初稿后可 activate_skill("文本润色") 进行深度润色
`;

/** Skill metadata for registration */
export const DRAFT_WRITING_SKILL_META = {
  name: '正文写作流程',
  summarys: ['完整的小说正文创作工作流。从大纲检查、角色状态同步、记忆规则查询，到章节撰写、润色、字数检查的端到端流程。'],
  tags: ['技能', '正文', '写作', '创作', '写章节', '润色'],
  category: '创作',
  tools: ['outline', 'character', 'memory'] as const,
  codeTrigger: 'draft_writing',
  textTriggers: ['写正文', '写章节', '开始写', '写第', '续写', '补写', '重写正文'],
};
