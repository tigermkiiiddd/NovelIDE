export const SKILL_OUTLINE_CREATION = `---
name: 大纲创建流程
category: 规划
tools: ['outline', 'character', 'relationship']
summarys: ['从零到一创建结构化小说大纲的事务流程。先召回项目素材与现有 Timeline，展示节点草案并等待用户确认，再把卷、章、事件、伏笔写入大纲系统，避免未确认内容污染结构化资产。']
tags: ["技能", "大纲", "创建大纲", "写大纲", "剧情规划", "故事结构", "大纲设计", "细纲", "章纲", "剧情节点", "情绪曲线", "伏笔", "反转"]
---

# 核心指令：大纲创建事务流程

## 触发条件

当用户要求创建新大纲、写大纲、做章纲、规划剧情、设计故事结构，或把粗略想法整理成可执行大纲时，激活此技能。

本技能负责流程和写入边界；剧情结构方法论优先参考「大纲构建」。不要在本技能里重复展开完整写作理论。

---

## 工作流总览

P1 召回与缺口确认 → P2 草案预览 → P3 用户确认 → P4 结构化写入 → P5 写后校验

此技能激活后已解锁 outline、character、relationship 工具类别。read 是基础文件能力，不需要作为技能工具类别声明。

---

## 引导树

激活本技能后，先按下面的树判断，不要直接进入写入。

\`\`\`
用户想要大纲
├─ A. 只是讨论方向/评估想法
│  └─ 不写入 Timeline，只给结构判断、风险和下一步建议
├─ B. 从零创建完整大纲
│  ├─ 先查现有卷/章/事件、角色、关系、设定
│  ├─ 缺关键约束 → 只问缺口
│  ├─ 信息足够 → 展示卷级/阶段级草案
│  └─ 用户确认 → 批量写入卷、章、事件、伏笔
├─ C. 补全已有大纲
│  ├─ 先定位已有卷/章/事件范围
│  ├─ 草案必须衔接已有结构
│  └─ 用户确认 → 只新增缺失部分，不重建已有内容
├─ D. 扩展某一卷/某几章
│  ├─ 读取目标范围的上下文和相邻章节
│  ├─ 展示局部节点草案
│  └─ 用户确认 → 写入目标范围，不影响其它卷章
└─ E. 修改已创建的大纲
   ├─ 先回读目标卷/章/事件
   ├─ 展示修改草案或差异点
   └─ 用户确认 → 使用 update，不重复 add
\`\`\`

### 路径选择原则

- 用户没有要求落库，默认走 A。
- 用户要求“创建/写入/生成到大纲系统”，才进入 B/C/D/E 的写入链路。
- 只要已有结构存在，优先按 C/D/E 局部处理，不要当成从零创建。
- 不确定范围时，先问范围，不要猜。
- 用户确认前，所有路径都只能输出草案、判断或问题，不能调用写入类 outline 工具。

---

## 相关 Skill 索引

本技能是“大纲创建事务流程”，不是所有大纲方法论的全集。进入具体设计问题时，按需激活下列 Skill；不要把这些方法论内容复制到本技能里。

| 遇到的问题 | 应激活的 Skill | 用法边界 |
|-----------|----------------|----------|
| 要设计整体剧情结构、黄金三章、卷纲、章纲、叠浪情绪曲线 | activate_skill("大纲构建") | 大纲设计的主方法论入口；本技能只负责确认与写入流程 |
| 要平衡主线、支线、暗线、多条叙事线 | activate_skill("线束编织") | 解决线索/故事线之间的编织和占比问题 |
| 要设计期待、钩子、延迟满足、读者追读动力 | activate_skill("期待感管理") | 用于节点钩子、期待建立、满足时机判断 |
| 要设计爽点密度、小中大爽点、高潮释放节奏 | activate_skill("爽点节奏管理") | 用于商业爽文节奏和情绪峰值安排 |
| 缺主角、反派、配角动机，或事件不像角色主动推动 | activate_skill("角色设计") | 先补“底色-追求-手段”，再回到大纲草案 |
| 已有角色在大纲中发生状态、关系、阵营变化 | activate_skill("角色状态追踪") | 用于检查角色连续性；真正更新仍用 character/relationship 工具 |
| 大纲事件依赖力量体系、势力格局、资源规则 | activate_skill("世界观构建") | 先确认规则约束，避免大纲事件破坏设定 |
| 用户给的方向混乱，存在多个互斥目标或隐藏约束 | activate_skill("深度思考方法论") | 用于收敛问题表示，不直接替代大纲写入 |
| 还没有项目基础设定，用户其实是在开新书 | activate_skill("项目初始化") | 先完成故事核、角色骨架、世界观入口，再创建大纲 |

### Skill 调用原则

- 已经明确只是写入事务时，不要额外激活一堆方法论 Skill。
- 草案质量不足时，先激活对应方法论 Skill 修正草案，再回到本技能的确认与写入流程。
- 不确定该用哪个 Skill 时，优先激活「大纲构建」作为剧情结构主入口。
- 同一轮里可以激活多个 Skill，但每个 Skill 必须服务一个明确缺口。

---

## P1：召回与缺口确认

### 1.1 先查已有资产

必须先确认当前项目状态，再判断本次是从零创建、补全已有大纲，还是扩展某一卷/某几章。

1. outline_getVolumes()：查看卷结构。
2. outline_getChapters()：查看章节列表。
3. 按需 outline_getEvents(chapterIndex)：查看已有事件。
4. query_relationships() 或 get_relationship_graph()：查看角色关系网络。
5. 按需读取角色档案、世界观、已有设定文件。

如果项目已有角色、世界观、旧大纲，新大纲必须衔接这些资产。不要只按聊天历史或通用套路生成。

### 1.2 只问缺失信息

不要机械追问固定问卷。用户已经给出的信息直接复用；只补问会影响大纲结构的缺口：

- 题材与读者期待不明确。
- 篇幅、卷数、章节范围不明确。
- 核心卖点或主线目标不明确。
- 结局、阶段转折、主角动机缺失。
- 本次写入范围不明确。

信息足够时，直接进入草案设计。

---

## P2：草案预览

### 核心门槛

用户确认前，禁止调用写入类 outline 工具创建卷、章、事件、伏笔。

先在对话中展示最小可审阅草案。草案不是正文，也不是完整设定文档，只用于让用户确认结构方向。

### 草案格式

每个节点至少包含：

\`\`\`
## 节点 [序号]：[节点标题]
- 剧情内核：[谁在什么情境下做了什么，导致什么结果]
- 推动者与动机：[谁主动推动，为什么]
- 冲突/阻碍：[外部阻碍、内部矛盾、价值冲突]
- 情绪曲线：[起始情绪] → [峰值情绪] → [收尾情绪]（-5 到 +5）
- 伏笔：[新埋/推进/回收；计划回收章节；关联已有伏笔 ID 如有]
- 转折/反转：[预期如何被打破，是否有公平铺垫]
- 章末钩子：[读者为什么继续看下一章]
\`\`\`

短篇可直接展示章级节点；中长篇先展示卷级或阶段级，确认后再展开章级。

---

## P3：用户确认

展示草案后必须停下确认：

> 这是大纲草案预览。请确认结构方向、情绪曲线、伏笔回收、反转和钩子是否符合预期。你确认后，我再写入结构化大纲；也可以指定要修改的节点。

只有用户明确表示“确认”“可以”“就这样”“按这个创建”等，才进入 P4。

---

## P4：结构化写入

### 4.1 首选写入方式：processOutlineInput

创建完整大纲或批量写入卷、章、事件时，优先使用 processOutlineInput。必须使用对象参数，不要使用字符串双参数写法。

\`\`\`
processOutlineInput({
  thinking: "用户已确认草案，准备写入结构化大纲",
  userInput: "【卷】...\\n【章节】...\\n【事件】...",
  mode: "add",
  instructions: "【来源】用户确认后的大纲草案\\n【目标】从零创建卷、章节、事件，并把事件中的伏笔写入 foreshadowing 字段\\n【状态】已查询现有卷/章/事件，确认本次写入范围\\n【禁止】不要创建草案外的额外章节或事件"
})
\`\`\`

instructions 必须把来源、目标、现有状态、禁止事项说清楚。SubAgent 不知道主 Agent 的前文分析，不能省略。

### 4.2 精确写入方式：outline_manageVolumes / outline_manageChapters / outline_manageEvents

当只需要少量精确改动时，可以直接使用管理工具：

1. outline_manageVolumes：创建或更新卷。
2. outline_manageChapters：创建或更新章节。
3. outline_manageEvents：创建或更新事件。

### 4.3 伏笔创建的正确路径

新伏笔不是通过 outline_manageForeshadowing(action: "create") 创建。

新伏笔必须随事件写入：

- 使用 processOutlineInput 时，在 userInput 和 instructions 中明确要求把伏笔写入事件的 foreshadowing 字段。
- 使用 outline_manageEvents 时，在 add/insert/update 的事件对象里填写 foreshadowing 数组。

新伏笔格式：

\`\`\`
{
  "content": "捡到神秘玉佩",
  "type": "planted",
  "plantedChapter": 1,
  "plannedChapter": 8,
  "hookType": "mystery",
  "strength": "medium",
  "tags": ["物品"]
}
\`\`\`

推进或回收已有伏笔时，必须先查询未完结伏笔，再使用 existingForeshadowingId：

\`\`\`
{
  "existingForeshadowingId": "foreshadow-xxx",
  "type": "developed",
  "content": "主角得知玉佩来自失落宗门",
  "tags": ["物品", "宗门"]
}
\`\`\`

outline_manageForeshadowing 只用于既有伏笔的 list、update、delete、update_planned。不要把它写成 create 入口。

### 4.4 角色状态与关系

如果大纲创建导致角色目标、关系、阵营、关键经历发生变化：

1. update_character_profile(characterName)：更新角色动态状态。
2. manage_relationships：更新角色关系变化。

不要为了形式更新所有角色；只更新被本次大纲实际改变的角色。

---

## P5：写后校验

写入后必须回读结构化结果：

1. outline_getVolumes()：确认卷已创建或更新。
2. outline_getChapters()：确认章节数量、标题、摘要正确。
3. 按范围 outline_getEvents(chapterIndex)：确认事件、情绪、角色、地点、伏笔字段落入正确章节。
4. outline_getUnresolvedForeshadowing()：确认新埋伏笔进入未完结列表，且 plannedChapter 合理。

发现写错范围、重复创建、伏笔未入库、章节摘要偏离草案时，立即用 update 修正，不要重新生成一套大纲。

---

## 反模式

| 反模式 | 表现 | 纠正 |
|--------|------|------|
| 未确认就写入 | 用户只是在讨论方向，Agent 已经创建卷章事件 | 草案阶段只输出预览，确认后再写入 |
| 固定问卷阻塞 | 用户信息已足够，仍机械追问四五个问题 | 只问影响结构的缺口 |
| 工具调用形状错误 | processOutlineInput("文本", "add") | 使用对象参数，并提供 instructions |
| 伏笔入口错误 | outline_manageForeshadowing(action: "create") | 新伏笔随 outline_manageEvents 或 processOutlineInput 的事件 foreshadowing 字段创建 |
| 方法论重复堆叠 | 在本技能里重复大篇幅叠浪、爽点、反转理论 | 本技能只保留流程和结构化写入边界，方法论交给「大纲构建」 |
| 只写文本不入库 | 伏笔写在摘要里，但没有进入 foreshadowing 字段 | 写事件时同步 foreshadowing 数组 |
`;

/** Skill metadata for registration */
export const OUTLINE_CREATION_SKILL_META = {
  name: '大纲创建流程',
  summarys: ['从零到一创建结构化小说大纲的事务流程。先召回项目素材与现有 Timeline，展示节点草案并等待用户确认，再把卷、章、事件、伏笔写入大纲系统，避免未确认内容污染结构化资产。'],
  tags: ['技能', '大纲', '创建大纲', '写大纲', '剧情规划', '故事结构', '细纲', '情绪曲线', '伏笔', '反转'],
  category: '规划',
  tools: ['outline', 'character', 'relationship'] as const,
  codeTrigger: 'outline_creation',
  textTriggers: ['创建大纲', '写大纲', '做大纲', '规划剧情', '设计大纲', '从零开始大纲', '新建大纲'],
};
